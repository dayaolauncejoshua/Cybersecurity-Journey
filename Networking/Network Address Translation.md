Topic: Network Address Translaion

What is NAT?
- is a process performed by a router that translates a device's private IP address into the router's public IP address for outgoing traffic, and translates incoming responses back to the correct private IP. This allows multiple devices on a private network to communicate with external networks such as the Internet using a single public IP address.