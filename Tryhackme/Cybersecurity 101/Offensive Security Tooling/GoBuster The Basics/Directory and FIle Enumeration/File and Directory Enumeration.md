**Gobuster dir Mode**

    dir mode is used to discover directories and files on a web server.
    It uses a wordlist and tries each word as a possible path.

    Simple command:
    
    gobuster dir -u http://TARGET -w wordlist.txt

    dir mode use a wordlist to discover hidden or unlinked directories and files on a web server.